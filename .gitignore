# Dependency directories
node_modules/
frontend/node_modules/
contracts/target/
contracts/verimed-contract/target/

# Next.js
frontend/.next/
frontend/out/

# Build and environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.DS_Store

# Soroban / Rust
# Cargo.lock is committed for contracts/verimed-contract (it's an application,
# not a library — pinning prevents ecosystem drift from breaking the build).
target/
**/*.rs.bk

project_spec.md